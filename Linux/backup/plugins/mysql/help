Plugin MySQL :

do mysql dump and synchronise it with SpiderOak. All databases except information_schema will be mysqldumped

--store="/dir/to/store" : Dir storage for mysql dump files
--user="user" : User for MySQL. Check your permissions for mysqldumping and databases list
--pass="password" : Password for MySQL


Example : exe.sh mysql --user="root" --pass="root"

Please don't use root user to do mysqldump. It's dangerous. Create user with minimum privileges to do mysql dump